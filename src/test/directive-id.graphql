type User {
    userId: ID! @id
    firstName: String
    lastName: String
    role: Role
}

type Group {
    name: String
}

type Moderator {
    moderatorId: ID!
    name: String
}

enum Role {
    ADMIN
    USER
    GUEST
}
