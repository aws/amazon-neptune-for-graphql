
import { jest } from '@jest/globals';
import { asyncProcess } from "../../neptune-for-graphql";

test('adds 1 + 2 to equal 3', () => {
    expect(1+2).toBe(3);
});


//test('asyncProcess', async () => {
//    expect(asyncProcess()).not.toBe(null);
//});